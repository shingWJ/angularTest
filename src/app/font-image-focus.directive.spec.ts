import { FontImageFocusDirective } from './font-image-focus.directive';

describe('FontImageFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new FontImageFocusDirective();
    expect(directive).toBeTruthy();
  });
});
