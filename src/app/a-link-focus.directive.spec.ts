import { ALinkFocusDirective } from './a-link-focus.directive';

describe('ALinkFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new ALinkFocusDirective();
    expect(directive).toBeTruthy();
  });
});
