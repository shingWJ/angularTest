import { VodPlayDirective } from './vod-play.directive';

describe('VodPlayDirective', () => {
  it('should create an instance', () => {
    const directive = new VodPlayDirective();
    expect(directive).toBeTruthy();
  });
});
