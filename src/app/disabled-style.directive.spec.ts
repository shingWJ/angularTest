import { DisabledStyleDirective } from './disabled-style.directive';

describe('DisabledStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledStyleDirective();
    expect(directive).toBeTruthy();
  });
});
